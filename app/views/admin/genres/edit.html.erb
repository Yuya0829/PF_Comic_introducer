<div class="container">
   <% if @genre.errors.any? %>
    　<%= @genre.errors.count %>errors prohibited this obj from being saved:
      <ul>
      <% @genre.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
      </ul>
    <% end %>

  <h1>ジャンル編集</h1>

    <br>

    <table>
      <tbody>
        <tr>
          <%= form_with model: @genre, url: admin_genre_path(@genre.id) do |f| %>
          <div class="row">
            <div class="col-lg-2">ジャンル名</div>

            <div class="col-lg-3"><%= f.text_field :name %></div>

            <div class="col-lg-2"><%= f.submit '変更を保存', class: "btn btn-success" %></div>

            <div class="col-lg-2"><%= link_to "削除する", admin_genre_path(@genre.id), class: "btn btn-danger", method: :delete %></div>
          </div>
          <% end %>
        </tr>
      </tbody>
    </table>

</div>