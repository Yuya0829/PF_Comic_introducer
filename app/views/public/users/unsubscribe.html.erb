<div style="text-align:center;">
   <br>
    <div>
        <h4><b>本当に退会しますか？</b></h4>

        退会すると、会員登録情報や</br>
        これまでの投稿履歴が閲覧できなくなります。</br>
        退会する場合は、「退会する」をクリックしてください。
    </div>

　　　<br>
      <div>
        <% if current_user %>
          <%= link_to "退会しない", user_path(@user.id), class: "btn btn-primary" %>
          <%= link_to "退会する", withdraw_users_path(user_id: @user.id), class: "btn btn-danger", method: :patch, data: { confirm: '退会しますか？' } %>
        <% elsif admin_signed_in? %>
          <%= link_to "退会させない", admin_users_path, class: "btn btn-primary" %>
          <%= link_to "退会させる", withdraw_users_path(user_id: @user.id), class: "btn btn-danger", method: :patch, data: { confirm: '退会しますか？' } %>
        <% end %>
      </div>

    </div>